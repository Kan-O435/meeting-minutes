# 今後の発展・課題

このドキュメントは、現在のプロトタイプの制約と、今後改善・検討すべき項目を整理したものです。実装の優先度付けや、次に着手する機能を検討する際の材料として使ってください。

## 目次

- [1. 音声入力の高度化](#1-音声入力の高度化)
  - [1-1. 相手（オンライン会議の相手側）の音声をどう取り込むか](#1-1-相手オンライン会議の相手側の音声をどう取り込むか)
  - [1-2. 認識精度をどう上げるか](#1-2-認識精度をどう上げるか)
- [2. 長時間の会議への対応](#2-長時間の会議への対応)
- [3. その他の課題・TODO](#3-その他の課題todo)
- [4. 優先度の目安](#4-優先度の目安)

## 1. 音声入力の高度化

### 1-1. 相手（オンライン会議の相手側）の音声をどう取り込むか

**現状の制約**：音声入力（Web Speech API）はマイクが物理的に拾える音しか文字起こしできない。対面会議では相手の声も拾えるが、オンライン会議でヘッドホン/イヤホンを使っている場合、相手の声はヘッドホンの中だけで鳴っており、マイクには入らないため文字起こしされない。

**検討した/検討すべき選択肢**：

| 案 | 概要 | メリット | 課題 |
| --- | --- | --- | --- |
| 会議プラットフォームの公式文字起こしを使う | Google Meet / Zoom等の字幕・文字起こし機能でテキスト化し、コピーしてB5に貼る | 実装コストほぼゼロ、精度が高い（各社が最適化済み） | プラットフォームごとに操作・出力形式が異なる。自動化ではなく手動コピーが前提 |
| 仮想オーディオデバイスでミックス | BlackHole（Mac）/ VB-CABLE（Windows）等でシステム出力とマイク入力を1つの入力にまとめる | ブラウザ側の実装は今のままで良い | 利用者ごとにOSレベルの追加セットアップが必要。音質劣化やハウリングのリスク |
| 会議プラットフォームのAPI連携 | Zoom Cloud Recording API、Google Meet REST API等から録音・文字起こしデータを取得 | 高精度・自動化できる | 各社API・OAuth連携の実装が必要。プラン制限（有料プランのみ等）がある場合がある |
| デスクトップ常駐アプリでのシステム音声キャプチャ | Electron等の別アプリでシステム音声を録音し、Apps Script側へ送る | 汎用性が高い | 「独自バックエンドを持たない」という現在の設計方針から逸脱する。実装・配布コストが大きい |

**現時点の結論**：プロトタイプの範囲では、オンライン会議は各プラットフォームの公式文字起こしをB5へ貼り付ける運用でカバーし、対面・自分の発言中心の会議では音声入力ページをそのまま使う、という使い分けを想定する。API連携は次フェーズの検討事項とする。

### 1-2. 認識精度をどう上げるか

**現状の制約**：ブラウザのWeb Speech APIは、ネットワーク状況・ブラウザの実装・話者のアクセントや専門用語によって認識精度が大きく変動する。特に固有名詞（社名・製品名・人名）や業界用語の誤認識が起きやすい。

**検討すべき改善案**：

1. **専用ASR（音声認識）サービスへの切り替え**
   - Google Cloud Speech-to-Text、OpenAI Whisper API等は、話者分離（誰が話したか）、句読点の自動付与、カスタム語彙登録などWeb Speech APIより高機能。
   - トレードオフ：追加のAPI費用・APIキー管理・実装コストが発生する。現在の「Apps Script + Gemini API」だけで完結する設計から一歩踏み出すことになる。
2. **カスタム語彙・辞書の登録**
   - 自社の製品名・頻出する固有名詞をあらかじめ登録できるASRサービスを使えば、誤認識を大きく減らせる。
3. **録音してバッチ処理する方式への切り替え**
   - リアルタイム認識ではなく、会議を録音 → 音声ファイルをアップロード → 高精度モデルでまとめて文字起こし、という流れにすると、リアルタイム性は失うが精度は上がる。
4. **Geminiによる事後補正**
   - 音声認識結果をそのまま使うのではなく、「誤字脱字を文脈から推測して補正して」という前処理をGeminiに行わせてから要約に回す、という2段階処理も検討できる（ただし、補正の過程で意味が変わってしまうリスクの管理が必要）。

## 2. 長時間の会議への対応

**現状の制約**：B5の文字起こし全文を、そのまま1回のプロンプトでGeminiへ送っている。会議が長時間に及ぶと、以下の問題が起きる。

- **入力トークン数の上限**：モデルのコンテキストウィンドウを超えると送信できない、または一部が切り捨てられる。
- **レイテンシ・コストの増大**：長文を1回で処理しようとすると応答が遅くなり、コストも比例して増える。
- **要約品質の低下**：文字起こしが長いほど、重要な論点が埋もれやすく、要約が総花的・表面的になりやすい。

**検討すべき対応案**：

1. **チャンク分割 + 階層要約（map-reduce方式）**
   - 文字起こしを一定の文字数・トークン数ごとに分割し、それぞれを個別に要約する（map）。
   - 各チャンクの要約をさらにまとめて最終要約を作る（reduce）。
   - ネクストアクション・追客タイミング・熱量評価についても、各チャンクから抽出した候補をマージ・重複排除する処理が必要になる。
2. **文字数・トークン数のしきい値チェック**
   - 「議事録を生成」実行時に文字起こしの長さを事前チェックし、しきい値を超える場合は自動でチャンク分割処理に切り替える、または利用者に警告を出す。
3. **区切りごとの逐次要約（会議中のリアルタイム処理）**
   - 会議中、一定時間・一定文字数ごとに区切って逐次要約し、会議終了時に区切りごとの要約を統合する方式。長時間会議でも待ち時間が発生しにくい。
4. **タスクに応じたモデルの動的選択**
   - 短い文字起こしは高速・低コストなモデル、長い文字起こしや複雑な議論は高精度なモデル、というようにGeminiProvider側でモデルを動的に切り替える仕組み。現在はモデル名を1箇所に固定しているが、将来的には「文字数に応じてモデルを選ぶ」ロジックに拡張できる設計になっている。

## 3. その他の課題・TODO

- **LLM Providerの追加**：`LlmService`はProviderを抽象化する設計にしてあるが、OpenAI・Claude用のProviderは未実装。追加する場合は`GeminiProvider.gs`と同じ`generateContent(prompt)`インターフェースを実装する。
- **複数ユーザー対応**：現在は`PropertiesService.getScriptProperties()`（スクリプト全体で共有）を使っており、単一ユーザーでの利用を想定している。複数人がそれぞれ別のAPIキーを使う場合は`UserProperties`への切り替えが必要（README「UserProperties と ScriptProperties について」を参照）。
- **音声入力Web APIのセキュリティ強化**：現在はトークンによる簡易認証のみ。レート制限、IPアドレス制限、トークンの自動ローテーション、アクセスログの記録などは未実装。
- **エラーハンドリングの充実**：Gemini API呼び出しのタイムアウトやレート制限時のリトライ機構（現在は失敗をそのまま利用者へ通知するのみ）。
- **話者分離・マルチスピーカー対応**：現在の文字起こしは「誰が発言したか」を区別しない1本のテキスト。話者ラベル付きの文字起こしに対応できれば、要約や熱量評価の精度向上が期待できる。
- **テストカバレッジの拡大**：現在のテストは`.gs`ファイル内の純粋関数のみが対象。`SpreadsheetApp`等のApps Script APIに依存する部分（シート作成、メニュー動作等）は、モックを用いた統合テストや、実際のApps Script環境でのE2Eテストの整備が課題。
- **GitHubへの自動push・CI導入**：開発中、ローカル環境のSSH鍵設定に起因してGitHubへのpushが度々ブロックされた。SSH認証の恒久的な解決、またはHTTPS + PAT（Personal Access Token）等の代替手段の検討、CIでのテスト自動実行の整備が望ましい。
- **UI/UXの改善**：議事録生成中の進捗表示（現在はB11のテキストのみ）、生成結果のプレビュー、モバイル端末での操作性向上など。
- **議事録のエクスポート・連携機能**：生成した議事録をPDF出力する、Slack/Chatworkへ通知する、Googleカレンダーの予定と紐付ける、といった外部連携。

## 4. 優先度の目安

あくまで目安であり、実際の優先順位は利用状況に応じて見直してください。

1. **高**：長時間会議のチャンク分割対応（1回の生成が失敗する実害に直結するため）
2. **高**：エラーハンドリングの充実（リトライ・タイムアウト個別対応）
3. **中**：オンライン会議の相手側音声の取り込み（公式文字起こしの活用ガイドをREADMEに追記するところから着手可能）
4. **中**：話者分離・専用ASRサービスへの切り替え検討
5. **低〜中**：Provider追加、複数ユーザー対応、エクスポート機能（利用規模が広がった段階で検討）
