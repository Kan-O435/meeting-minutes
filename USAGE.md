# 使い方ガイド

このドキュメントは、AI議事録プロトタイプの**日々の使い方**をまとめたものです。インストールやApps Scriptへのデプロイなど、初回セットアップの手順は [README.md](./README.md) の「セットアップ手順」を参照してください。

## 目次

- [基本の流れ](#基本の流れ)
- [メニュー一覧](#メニュー一覧)
- [シートのレイアウト](#シートのレイアウト)
- [1. 会議を開始する](#1-会議を開始する)
- [2. 文字起こしを入力する](#2-文字起こしを入力する)
- [3. 議事録を生成する](#3-議事録を生成する)
- [4. 会議を終了する](#4-会議を終了する)
- [5. シートをクリアする](#5-シートをクリアする)
- [APIキーの確認・変更](#apiキーの確認変更)
- [要約プロンプトのカスタマイズ](#要約プロンプトのカスタマイズ)
- [音声入力ページ（ローカル）の使い方](#音声入力ページローカルの使い方)
- [よくあるつまずきポイント](#よくあるつまずきポイント)

## 基本の流れ

```text
会議を開始
  → B5へ文字起こしを入力（手動 / OS音声入力 / 音声入力ページ）
    → 議事録を生成（B7・B9・B13・B15が埋まる）
      → 会議を終了（B3に終了日時が入り、未生成なら自動で議事録生成）
```

「議事録を生成」は会議の途中で何度でも実行できます。「会議を終了」を押すと、その時点のB5の内容を元にもう一度自動で生成が走ります。

## メニュー一覧

スプレッドシートを開くと、メニューバーに「**AI議事録**」が表示されます。

| メニュー項目 | 内容 |
| --- | --- |
| 会議を開始 | 新しい会議シートを一番左に作成し、B5を選択状態にする |
| 議事録を生成 | B5の文字起こしから要約・ネクストアクション等をB7〜B15へ生成する |
| 会議を終了 | B3へ終了日時を記録し、未生成なら議事録生成も行う |
| APIキーを設定 | Gemini APIキーを登録する |
| APIキー設定状況を確認 | APIキーが設定済みかどうかだけを表示する（値は表示しない） |
| 要約プロンプトを編集 | Geminiへ送るプロンプトを自分で書き換える |
| 現在の会議シートをクリア | 会議名・終了日時・文字起こし・生成結果をクリアする（開始日時は残る） |
| 音声入力ページのURLを設定 | 音声入力ページ（`npm run voice-input`等）のURLを登録する |
| 音声入力トークンを表示 | 音声入力ページに貼り付けるトークンを表示する（未発行なら発行する） |
| 音声入力トークンを再発行 | トークンを無効化して新しいものを発行する |
| 音声入力ページを開くリンクを表示 | 現在の会議シート向けの音声入力ページのリンクを表示する |

## シートのレイアウト

| セル | 内容 | 誰が/何が書き込むか |
| --- | --- | --- |
| B1 | 会議名 | 利用者（任意入力） |
| B2 | 開始日時 | 「会議を開始」時に自動記録 |
| B3 | 終了日時 | 「会議を終了」時に自動記録 |
| B5 | 文字起こし | 利用者（手動 / 音声入力） |
| B7 | 会議の要約 | Gemini（「議事録を生成」時） |
| B9 | ネクストアクション | Gemini |
| B11 | 処理状態（入力中／生成中／完了／エラー） | アプリが自動更新 |
| B13 | 追客タイミング | Gemini |
| B15 | 先方の熱量・反応評価 | Gemini |

## 1. 会議を開始する

「AI議事録」→「**会議を開始**」を選択します。

- `会議_YYYYMMDD_HHmmss` という名前のシートが一番左に作成されます。
- 同名のシートがある場合は自動で連番が付きます（`_2`, `_3`…）。
- B5（文字起こし欄）が選択された状態になるので、そのまま入力を始められます。

## 2. 文字起こしを入力する

以下のいずれかの方法でB5に文字起こしを入力します。

1. **直接入力**：B5セルに普通にタイピングする。
2. **OS標準の音声入力**：B5を選択したまま、Mac/Windowsの音声入力機能を使う（詳細は[README.md](./README.md#音声入力)）。
3. **音声入力ページ**：`npm run voice-input` で起動したローカルページから、ブラウザのマイクを使って認識→「シートへ反映」でB5へ書き込む（下記[音声入力ページ（ローカル）の使い方](#音声入力ページローカルの使い方)を参照）。

複数回に分けて入力・反映しても、内容は改行で追記されていきます。

## 3. 議事録を生成する

B5に文字起こしを入力した状態で、「AI議事録」→「**議事録を生成**」を選択します。

1. 会議シート以外では実行されません。
2. B5が空、またはGemini APIキー未設定の場合は警告が出て終了します。
3. B11が「生成中」→ 成功時「完了」／失敗時「エラー」に変わります。
4. 成功すると、B7・B9・B13・B15が埋まります。

**担当者・期限（B9）や追客タイミング（B13）・熱量評価（B15）は、文字起こしから根拠が読み取れない場合、必ず「未定」または「不明」になります。** AIが憶測で埋めることはありません。

## 4. 会議を終了する

「AI議事録」→「**会議を終了**」を選択すると、B3に現在日時が記録されます。B5に文字起こしがあれば、続けて議事録生成も行われます。終了日時が既に入っている場合は上書き確認が出ます。

## 5. シートをクリアする

「AI議事録」→「**現在の会議シートをクリア**」を選択すると、確認ダイアログの後、B1・B3・B5・B7・B9・B13・B15がクリアされます。B2（開始日時）は保持されます。

## APIキーの確認・変更

- 「APIキーを設定」：ダイアログにGemini APIキーを入力して保存します。値は保存後、画面にもログにも表示されません。
- 「APIキー設定状況を確認」：`Gemini APIキー：設定済み` のように状態だけを表示します。

## 要約プロンプトのカスタマイズ

「要約プロンプトを編集」を開くと、現在使用中のプロンプト全文がテキストエリアに表示されます。

- 文字起こしを差し込みたい位置に `{{TRANSCRIPT}}` と書く（書き忘れても末尾に自動追加）。
- 「保存」でカスタムプロンプトとして保存、「デフォルトに戻す」で組み込みのデフォルトへ戻せます。
- `summary`・`nextActions`・`followUp`・`engagement` を含むJSON出力の指示は残しておく必要があります（消すとB7〜B15への書き込みが失敗します）。

詳しくは[README.md](./README.md#要約プロンプトのカスタマイズ)を参照してください。

## 音声入力ページ（ローカル）の使い方

**初回だけ**

1. `npx clasp deploy --description "voice-input api"` でWebアプリをデプロイし、exec URLを控える。
2. `npm run voice-input` を実行 → `http://localhost:8787/` が起動。
3. スプレッドシートで「音声入力ページのURLを設定」→ `http://localhost:8787` を登録。
4. 「音声入力トークンを表示」→表示された値をコピー。
5. `http://localhost:8787/` を開き、「設定」に手順1のURLと手順4のトークンを貼り付けて保存。

**使うとき**

1. `npm run voice-input` を実行（起動していなければ）。
2. 会議シート上で「音声入力ページを開くリンクを表示」→リンクをコピーして開く。
3. 「音声入力開始」→話す→「音声入力停止」→内容を確認して「シートへ反映」。

詳細・トラブルシューティングは[README.md](./README.md#音声入力)を参照してください。

## よくあるつまずきポイント

| 症状 | 対処 |
| --- | --- |
| 「現在のシートは会議シートではありません」と出る | `会議_...` という名前のシート上で操作する |
| 「Gemini APIキーが設定されていません」と出る | 「APIキーを設定」から登録する |
| 「反映先シートが未設定です」（音声入力ページ） | 「音声入力ページを開くリンクを表示」から開き直すか、ページの「設定」でスプレッドシートID・シート名を直接入力する |
| 「マイクの利用が許可されていません」（スプレッドシートのサイドバー等） | Apps Script配信ページの既知の制限。音声入力ページ（ローカル）を使う |
| 議事録生成でエラーになる | B11が「エラー」の場合、ダイアログのメッセージを確認。詳細は[README.md トラブルシューティング](./README.md#トラブルシューティング)へ |
